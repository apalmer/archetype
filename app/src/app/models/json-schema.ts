export interface JsonSchema {
    // $schema:string;
    // $id:string;
    // title:string;
    // description:string;
    // type: "object" | "array",
    properties: any,
    // required:string[]
}

export interface JsonSchemaProperty {
    type:string
}