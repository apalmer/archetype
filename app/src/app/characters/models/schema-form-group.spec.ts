import { SchemaFormGroup } from './schema-form-group';

describe('SchemaFormGroup', () => {
  it('should create an instance', () => {
    expect(new SchemaFormGroup(null,null,null)).toBeTruthy();
  });
});
